# URL de conexão ao banco de dados: especificar host, porta e nome da base de dados
spring.datasource.url=jdbc:postgresql://localhost:5432/db_turismo-api

#Usuário de acesso ao banco
spring.datasource.username=postgres

#Senha do usuário
spring.datasource.password=resende123

#Criar o esquema do banco de dados
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true

#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL95Dialect
#spring.jpa.properties.hibernate.dialect=org.hibernate.spatial.dialect.postgis.PostgisDialect

spring.datasource.initialization-mode=always
spring.datasource.platform=postgresql

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

spring.jpa.open-in-view=false

jwt_secret=fsdfs#5345ggd*465sdf*gs*df
jwt_expires=3600